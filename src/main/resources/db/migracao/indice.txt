# ÍNDICE DAS MIGRAÇÕES — a ordem é DECLARADA, nunca deduzida.
#
# Varredura de diretório muda de resultado entre a IDE e o jar, e ordem de DDL
# que depende do empacotamento produz bancos diferentes a partir do mesmo
# código. Arquivo esquecido aqui falha ALTO, em vez de simplesmente não ser
# aplicado — que é o silêncio que esta lista existe para impedir.
#
# Uma linha por arquivo, no formato V<numero>__<descricao>.sql
# Migração aplicada é IMUTÁVEL: correção vira migração nova.
#
# A NUMERAÇÃO RECOMEÇOU em 03/09/2026, e isso não é descuido: o motor mudou de
# PostgreSQL para SQLite e cinco das nove tabelas deixaram de existir. As
# migrações antigas não executam no motor novo. Elas estão preservadas, com o
# motivo, em `docs/historico-migracoes-postgres/`.

V001__esquema_inicial.sql
V002__sem_cadastro_nem_fila.sql
